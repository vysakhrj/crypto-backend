"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
var consola_1 = __importDefault(require("consola"));
var error_reporter_1 = require("./error-reporter");
module.exports = function (moduleName) {
    var mainReporter = new consola_1.default.FancyReporter();
    // Currently we can set reporters using env variables
    if (process.env.ENABLE_JSON_LOGS === "true") {
        mainReporter = new consola_1.default.JSONReporter();
    }
    return consola_1.default.create({
        // Here we can pass additional options for logger configuration
        // level: 4
        reporters: [
            mainReporter,
            new error_reporter_1.ConsolaErrorReporter(),
        ],
    }).withTag(moduleName);
};
