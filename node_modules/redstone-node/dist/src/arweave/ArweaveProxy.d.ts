import Arweave from "arweave/node";
import Transaction from "arweave/node/lib/transaction";
import { JWKInterface } from "arweave/node/lib/wallet";
import { SmartWeave } from "redstone-smartweave";
export default class ArweaveProxy {
    jwk: JWKInterface;
    arweave: Arweave;
    smartweave: SmartWeave;
    constructor(jwk: JWKInterface);
    sign(strToSign: string): Promise<string>;
    getAddress(): Promise<string>;
    getBalance(): Promise<number>;
    prepareUploadTransaction(tags: any, data: any): Promise<Transaction>;
    postTransaction(tx: Transaction): Promise<void>;
}
