import { Broadcaster } from "./Broadcaster";
import { PriceDataSigned, SignedPricePackage } from "../types";
export declare class HttpBroadcaster implements Broadcaster {
    private readonly broadcasterURLs;
    constructor(broadcasterURLs?: string[]);
    broadcast(prices: PriceDataSigned[]): Promise<void>;
    broadcastPricePackage(signedData: SignedPricePackage, providerAddress: string): Promise<void>;
}
