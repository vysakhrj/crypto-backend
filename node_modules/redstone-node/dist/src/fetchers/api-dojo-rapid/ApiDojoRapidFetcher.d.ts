import { BaseFetcher } from "../BaseFetcher";
import { FetcherOpts, PricesObj } from "../../types";
export declare class ApiDojoRapidFetcher extends BaseFetcher {
    constructor();
    fetchData(tokens: string[], opts?: FetcherOpts): Promise<any>;
    extractPrices(responses: any): Promise<PricesObj>;
}
