import { BaseFetcher } from "../BaseFetcher";
import { PricesObj } from "../../types";
export declare class PangolinFetcher extends BaseFetcher {
    private readonly baseTokenSymbol;
    protected retryForInvalidResponse: boolean;
    constructor(name: string, baseTokenSymbol: string);
    fetchData(symbols: string[]): Promise<any>;
    validateResponse(response: any): boolean;
    extractPrices(response: any): Promise<PricesObj>;
    private getPairIdsForSymbols;
}
