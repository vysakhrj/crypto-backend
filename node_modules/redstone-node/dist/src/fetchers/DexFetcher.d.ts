import { PricesObj } from "../types";
import { BaseFetcher } from "./BaseFetcher";
interface SymbolToPairId {
    [symbol: string]: string;
}
export declare class DexFetcher extends BaseFetcher {
    private readonly subgraphUrl;
    private readonly symbolToPairIdObj;
    protected retryForInvalidResponse: boolean;
    constructor(name: string, subgraphUrl: string, symbolToPairIdObj: SymbolToPairId);
    fetchData(symbols: string[]): Promise<any>;
    validateResponse(response: any): boolean;
    extractPrices(response: any, symbols: string[]): Promise<PricesObj>;
    private convertSymbolsToPairIds;
}
export {};
