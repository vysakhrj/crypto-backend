import { BaseFetcher } from "../BaseFetcher";
import { PricesObj } from "../../types";
export declare class VertoFetcher extends BaseFetcher {
    constructor();
    fetchData(symbols: string[]): Promise<any>;
    extractPrices(responses: any): Promise<PricesObj>;
}
