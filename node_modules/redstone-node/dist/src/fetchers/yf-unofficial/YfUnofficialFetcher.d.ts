import { PricesObj } from "../../types";
import { BaseFetcher } from "../BaseFetcher";
export declare class YfUnofficialFetcher extends BaseFetcher {
    private yahooFinanceProxy;
    constructor();
    fetchData(symbols: string[]): Promise<any>;
    extractPrices(response: any): Promise<PricesObj>;
}
